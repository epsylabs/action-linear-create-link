{
  "name": "action-linear-create-link",
  "version": "1.0",
  "description": "Create link attached to given resource",
  "main": "src/main.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "format": "prettier --write **/*.ts",
    "package": "ncc build --license licenses.txt",
    "all": "yarn format && yarn build && yarn package"
  },
  "keywords": [],
  "author": "nrpx",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@linear/sdk": "^2.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "prettier": "^3.0.0",
    "@types/node": "^20.4.1",
    "typescript": "^5.1.6"
  }
}
